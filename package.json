{
  "name": "fog-wave-grid",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://<Sachin01pr>.github.io/<fog_wave_grid>",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    // ... your dependencies
  },
  "devDependencies": {
    "gh-pages": "^3.0.0"
  }
}